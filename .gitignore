# .NET build artifacts (dotnet-outputs/ holds reference/test migrations — source only, no build output)
bin/
obj/
*.dll
*.pdb

# accoreconsole integration test artifacts (regenerated by RunIntegrationTests.ps1)
*_generated.scr
TestResults.xml
TestReport.html
ErrorReports/
InternalTrace.*.log

# dotnet new template install cache (if installed locally for testing)
*.nupkg
